- args:
    sql: CREATE TABLE "user"("hasura_id" integer NOT NULL, "name" text NOT NULL, "email"
      text NOT NULL, "gender" text NOT NULL, "profile_file_id" text NOT NULL, PRIMARY
      KEY ("hasura_id") )
  type: run_sql
- args:
    name: user
  type: add_existing_table_or_view
