- args:
    sql: CREATE TABLE "match"("id" serial NOT NULL, "hasura_id" integer NOT NULL,
      "like_user_id" integer NOT NULL, PRIMARY KEY ("id") )
  type: run_sql
- args:
    name: match
  type: add_existing_table_or_view
