- args:
    sql: CREATE TABLE "match"("id" integer NOT NULL, "hasura_id" integer NOT NULL,
      "LikeUser_id" integer NOT NULL, PRIMARY KEY ("id") )
  type: run_sql
- args:
    name: match
  type: add_existing_table_or_view
